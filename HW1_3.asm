		ORG 0
		MOV		DPTR, #300H
DF:		MOV		R1, #0
		MOV		R0, #0
		
LP:		MOV		R5, #100
LP1:	MOV		A, R1
		MOVC	A, @A + DPTR
		JNB		P2.0, DF
		MOV		P1, A
		CALL	DELAY
		MOV		P1, #0FFH
		CLR		P3.3		
		
		MOV		A, R0
		MOVC	A, @A + DPTR
		JNB		P2.0, DF
		MOV		P1, A
		CALL	DELAY
		MOV		P1, #0FFH
		SETB	P3.3
		DJNZ	R5, LP1		
		INC		R0		
		
		CJNE	R0, #10, LP2		
		CJNE	R1, #9, LP3
		MOV		P1,	#0FFH				
FIN:	SJMP	FIN
LP3:	MOV		R0, #0
		INC		R1
		LJMP	LP

LP2:	LJMP	LP
	
DELAY:	MOV		R2, #100
AGAIN:	MOV		R3, #11
HERE:	NOP
		NOP	
		DJNZ	R3, HERE
		DJNZ	R2, AGAIN
		RET

		ORG 300H
TABLE:
DB		192,249,164,176,153,146,130,216,128, 144

END
